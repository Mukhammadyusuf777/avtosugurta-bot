{
  "name": "avtosugurta-bot",
  "version": "1.0.0",
  "description": "Cloudflare Workers Telegram bot for managing car insurance clients with Cloudflare D1",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "npx tsx tests/parser.test.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create avtosugurta-db",
    "d1:migrate:local": "wrangler d1 execute avtosugurta-db --local --file=./migrations/0001_schema.sql && wrangler d1 execute avtosugurta-db --local --file=./migrations/0002_seed.sql",
    "d1:migrate:remote": "wrangler d1 execute avtosugurta-db --remote --file=./migrations/0001_schema.sql && wrangler d1 execute avtosugurta-db --remote --file=./migrations/0002_seed.sql"
  },
  "keywords": [
    "telegram-bot",
    "cloudflare-workers",
    "cloudflare-d1",
    "insurance",
    "uzbekistan"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.111.0"
  }
}
